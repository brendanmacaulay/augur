VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (used by the api/draft-risk serverless function).
# Do NOT prefix with VITE_ — that would expose it to the browser bundle.
ANTHROPIC_API_KEY=
